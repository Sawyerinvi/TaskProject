using UnityEngine;

public class _11VectorTask : MonoBehaviour
{
    //Перед выполнение задания повесте данный скрипт на обьект Target на сцене
    //и убедитесь что на нем нет других скриптов
    //По завершению задания в коде не должно быть ни одной ошибки компиляции, которая не позволяет
    //скомпилировать код. Такой код по умолчанию является неверным

    public Transform cubeTransform;
    public float speed = 5f;

    //Задание 1. Создай переменную типа Vector3 с именем right и присвой ей значение new Vector3(1,0,0)

    //Задание 2. В Update добавь к позиции куба эту переменную right, умноженную на speed и Time.deltaTime

    //Задание 3. Создай переменную Vector3 up и сделай так, чтобы куб теперь ехал строго вверх

    //Задание 4. Создай переменную Vector3 forward и сделай движение строго вперёд

    //Задание 5. Создай переменную Vector3 diagonal и задай ей значение new Vector3(1,0,1)
    //    → куб поедет по диагонали вправо-вперёд (но быстрее чем speed)

    //Задание 6. Добавь после создания diagonal одну строку: diagonal = diagonal.normalized;
    //    → теперь скорость по диагонали будет точно равна speed

    //Задание 7. Сделай публичную переменную public Vector3 moveDirection;
    //    и используй её вместо всех предыдущих переменных (перетаскивай значения в инспекторе)

    //Задание 8. Используй готовые векторы Unity: Vector3.right, Vector3.up, Vector3.forward 
    //    вместо new Vector3(...) — код станет короче

    void Update()
    {
        
    }
}