using UnityEngine;

public class _7DeltaTimeTask : MonoBehaviour
{
    //Перед выполнение задания повесте данный скрипт на обьект Target на сцене
    //и убедитесь что на нем нет других скриптов
    //По завершению задания в коде не должно быть ни одной ошибки компиляции, которая не позволяет
    //скомпилировать код. Такой код по умолчанию является неверным

    public float timer1 = 0f;
    public float health = 200f;
    public float value = 0f;
    public float damagePerSecond = 40f;
    public float regenPerSecond = 15f;

    //Задание 1. Каждый кадр прибавляй к timer1 значение Time.deltaTime

    //Задание 2. Когда timer1 станет больше или равно 1.0 — выведи в консоль "Прошла 1 секунда!" 
    //и обнули timer1 обратно в 0

    //Задание 3. Сделай так, чтобы сообщение из задания 2 выводилось ровно каждые 2 секунды 
    //(сравнивай с 2.0f вместо 1.0f)

    //Задание 4. Каждый кадр отнимай от health значение damagePerSecond * Time.deltaTime
    //Это будет означать, что объект получает ровно 40 единиц урона в секунду

    //Задание 5. Добавь восстановление: каждый кадр прибавляй к health значение 
    //regenPerSecond * Time.deltaTime (одновременно с уроном из задания 4)

    //Задание 6. Сделай переменную value, которая плавно увеличивается со скоростью 10 единиц в секунду 
    //(value += 10f * Time.deltaTime)

    //Задание 7. Когда value станет больше или равно 100 — выведи "Значение достигло 100!" 
    //и больше не увеличивай value
    void Update()
    {


    }
}